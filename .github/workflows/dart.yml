name: "Build & Release (Windows)"

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    name: Build APK & AAB (Windows)
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # ---------- FIXES ----------
      - name: Kill Java locks
        run: |
          taskkill /F /IM java.exe || echo "No java running"

      - name: Disable Gradle parallel execution
        run: echo "org.gradle.parallel=false" >> android/gradle.properties

      - name: Disable R8 (Windows CI Fix)
        run: echo "android.enableR8=false" >> android/gradle.properties

      - name: Clean Project
        run: flutter clean
      # ----------------------------

      - name: Flutter Pub Get
        run: flutter pub get

      - name: Build APK (Split ABI)
        run: flutter build apk --release --split-per-abi

      - name: Build AppBundle (AAB)
        run: flutter build appbundle --release

      - name: Upload to GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: |
            build\app\outputs\apk\release\*
            build\app\outputs\bundle\release\app-release.aab
          tag: v1.0.${{ github.run_number }}
